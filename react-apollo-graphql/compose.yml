services:
  node-app:
    build: .
    container_name: node-app-graphql
    volumes:
      - .:/app
    working_dir: /app
    ports:
      - "3000:3000"
      - "4000:4000"
    # Mantém o container rodando para criar a aplicação
    command: ["sh", "-c", "cd server && node server.js"]
