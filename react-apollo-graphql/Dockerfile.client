FROM node:20-alpine

WORKDIR /app

# Copia apenas package.json primeiro (cache layer)
COPY client/package*.json ./

# Instala dependências
RUN npm install

# Copia o resto do código
COPY client/ ./

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]